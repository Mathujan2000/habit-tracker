name: Build and Push Laravel Docker Image

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.mattoe }}
          password: ${{ secrets.dckr_pat__GX6E38tRzp1HEPmNMUpW6X8tKM }}

      - name: Build the Docker image
        run: docker build -t ${{ secrets.mattoe }}/laravel-app:latest .

      - name: Push the Docker image
        run: docker push ${{ secrets.mattoe }}/laravel-app:latest
